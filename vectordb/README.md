# VentureScout VectorDB

This directory is designated for persistent vector storage.

### Supported Engines:
- ChromaDB (Local / Containerized)
- Pinecone (Cloud)

### Schema:
- Collection: `startup_knowledge_base`
- Distance Metric: `cosine`
- Embeddings: `Google text-embedding-004`